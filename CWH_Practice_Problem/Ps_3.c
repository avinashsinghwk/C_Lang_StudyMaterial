#include <stdio.h>
int main()
{
    printf("In this lecture we will be solving practice problem 3");

    // Question 1. What will be the output of the program
    // int a = 10;
    // if(a = 11){
    //     printf("\nI am 11");
    // }
    // else{
    //     printf("\nI am not 11");
    // }

    // Question 2. Write a program to find out whether a student is pass or fail, if it requires total 40% and atleast 33% in each subject to pass.Assuem 3 subjects and take marks from the user input
    // int English, Hindi, Math, Tobtained;
    // printf("\nEnter marks of English : ");
    // scanf("%d",&English);
    // printf("Enter marks of Hindi : ");
    // scanf("%d",&Hindi);
    // printf("Enter marks of Math : ");
    // scanf("%d",&Math);
    // Tobtained = English + Hindi + Math;
    // if(English >= 33 && Hindi >= 33 && Math >= 33 && Tobtained >= 120){
    //     printf("You are pass");
    // }
    // else{
    //     printf("You are not pass");
    // }

    // Question 3. Calcualte Income tax paid by an Employee
    // long income;
    // double tax;
    // printf("\nEnter your income in a year : ");
    // scanf("%d", &income);
    // if (income <= 250000)
    // {
    //     printf("You don't need to pay any tax.");
    // }
    // else if (income > 250000 && income <= 500000)
    // {
    //     tax = ((income - 250000) * 5.0 / 100);
    // }
    // else if (income > 500000 && income <= 1000000)
    // {
    //     tax = (((500000.0 - 250000) * 5.0 / 100) + ((income - 500000) * 20.0 / 100));
    // }
    // else
    // {
    //     tax = (((500000.0 - 250000) * 5.0 / 100) + ((1000000.0 - 500000) * 20.0 / 100) + ((income - 1000000) * 30.0 / 100));
    // }
    //     printf("you have to pay %f Rs tax.", tax);

    // Question 4. Find the the year entered by the user is a leap year or not
    // int year;
    // printf("\nEnter a year : ");
    // scanf("%d", &year);
    // if (year % 4 == 0 && year % 100 != 0)
    // {
    //     printf("yes, %d is a leap year", year);
    // }
    // else
    // {
    //     printf("No, %d is not a leap year", year);
    // }

    // Question 5. Find the character entered by the user is lowercase or not
    // char ch;
    // printf("\nEnter a character : ");
    // scanf("%c",&ch);
    // if(ch >= 97 && ch <= 122){
    //     printf("Yes, the character you typed is lowercase");
    // }
    // else{
    //     printf("No, the character you typed is uppercase");
    // }
    // Note: ASCII value of lowercase alphabet ranges between 97 to 122

    // Question 6. Find the greatest of four number entered by the user
    // int p, q, r, s;
    // printf("\nEnter 1st number : ");
    // scanf("%d",&p);
    // printf("Enter 2nd number : ");
    // scanf("%d",&q);
    // printf("Enter 3rd number : ");
    // scanf("%d",&r);
    // printf("Enter 4th number : ");
    // scanf("%d",&s);
    // if(p > q && p > r && p > s){
    //     printf("%d is greater",p);
    // }
    // else if(q > p && q > r && q > s){
    //     printf("%d is greater",q);
    // }
    // else if(r > q && r > p && r > s){
    //     printf("%d is greater",r);
    // }
    // else{
    //     printf("%d is greater",s);
    // }

    return 0;
}