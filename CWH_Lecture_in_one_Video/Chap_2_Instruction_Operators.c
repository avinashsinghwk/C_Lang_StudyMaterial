#include <stdio.h>
#include <math.h>
int main()
{
    printf("In this lecture we will be studying about instructions and operators");

    // Type declaration instruction
    // int a = 4, b, c;
    // b = c = a;
    // printf("\nThe value of a is %d",a);
    // printf("\nThe value of b is %d",b);
    // printf("\nThe value of c is %d",c);

    // float z = 1.9;
    // float y = 8.1 + z;
    // printf("\nThe value of y = %f",y);

    // Arithmetic declaration instruction
    // int j = 4, l = 8;
    // printf("\nThe value of j + l = %d",j + l);
    // printf("\nThe value of j - l = %d",j - l);
    // printf("\nThe value of j * l = %d",j * l);
    // printf("\nThe value of l / j = %d",l / j);
    // printf("\nWhen l is diveded by j it leaves a remainder of %d",l % j);
    // printf("\nWhen -9 is diveded by 2 it leaves a remainder of %d",-9 % 2);
    // printf("\nWhen -9 is diveded by -2 it leaves a remainder of %d",-9 % -2);
    // printf("\nWhen 9 is diveded by -2 it leaves a remainder of %d",9 % -2);  // Because sign of numerator is taken
    // printf("\nThe value of 2 to the power 10 is %f",pow(2,10));

    // Type Conversion
    // printf("\nThe value of 6 + 4 = %d", 6 + 4);
    // printf("\nThe value of 6 + 4.3 = %f", 6 + 4.3);
    // printf("\nThe value of 6.1 + 4.3 = %f", 6.1 + 4.3);
    // printf("\nThe value if 5 / 2 = %d", 5 / 2);

    // Operator Precedence
    // int q = 4, r = 9;
    // printf("\nThe value of 3q - 8r = %d",3 * q - 8 * r);
    // if the operators of same precedence present in the equation then we see associativity

    // Control Instructions
    // In the control instruction we have if else, switch case, loops 






    return 0;
}